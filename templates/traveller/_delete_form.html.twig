<form class="form-control" method="post" action="{{ path('app_traveller_delete', {'id': traveller.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ traveller.id) }}">
    <button class="btn">Delete</button>
</form>
