{% extends 'base.html.twig' %}

{% block title %}Traveller{% endblock %}

{% block body %}
    <h1>Traveller</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ traveller.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ traveller.name }}</td>
            </tr>
            <tr>
                <th>Surnames</th>
                <td>{{ traveller.surnames }}</td>
            </tr>
            <tr>
                <th>Dni</th>
                <td>{{ traveller.dni }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ traveller.phone }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_traveller_index') }}">back to list</a>

    <a href="{{ path('app_traveller_edit', {'id': traveller.id}) }}">edit</a>

    {{ include('traveller/_delete_form.html.twig') }}
{% endblock %}
